# podman configuration; see containers.conf(5)

[engine]

# `podman compose` is a thin wrapper that shells out to whichever provider it
# finds first, and docker-compose wins by default. On WSL that resolves to
# Docker Desktop's Windows binary, which refuses to run inside the distro.
compose_providers = ["podman-compose"]

compose_warning_logs = false
